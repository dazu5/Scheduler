{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the main + pill windows: core plugin defaults (which include core:window:allow-start-dragging — needed for the issue #9 pill drag) plus tauri-plugin-dialog (file picker for v4 JSON import in issue #12). The pill window must be explicitly listed here, otherwise getCurrentWindow().startDragging() silently no-ops in the pill webview.",
  "windows": ["main", "pill"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
