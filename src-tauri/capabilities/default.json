{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the main window: core plugin defaults plus tauri-plugin-dialog (file picker for v4 JSON import in issue #12).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
